[project]
name = "basket"
version = "0.1.0"
description = "Basketball tournament organizer for Basket UGA"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0.0",
    "streamlit>=1.32.0",
    "streamlit-mermaid-interactive>=0.1.12",
]

[tool.uv]
package = false
